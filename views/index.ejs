<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen p-10 bg-zinc-800 block">
        <h1 class="text-center text-2xl text-slate-200 m-10">Create Tasks</h1>
        <form action="/create-tasks" method="POST" class="flex flex-col">
            <input type="text" name="title" class="w-full p-3 border-none text-slate-200 bg-slate-900 rounded-md mb-3 caret-emerald-400 focus:outline-emerald-400" placeholder="Title..">
            <textarea name="description" id="" cols="30" rows="10" class="w-full p-3 border-none text-slate-200 resize-none  bg-slate-900 rounded-md caret-emerald-400 focus:outline-emerald-400" placeholder="Description"></textarea>
            <input type="submit" class="bg-blue-800 w-40 rounded-md p-3 mt-4 font-bold text-slate-100" value="Create">
        </form>
        <%if(files.length > 0){%>
            <h1 class="text-center text-2xl text-slate-200">Tasks:</h1>
        <%}%>
        <div class="flex flex-wrap mt-5 gap-3">
            <%if(files.length > 0){%>
            <% files.forEach((title)=>{ 
                const [value,] = title.split('.');
                %>
            <a href="/read/<%=value%>" class="bg-slate-900 min-w-72 p-2 rounded-lg hover:scale-105 shadow-xl hover:transition-all hover:ease-in-out delay-150 cursor-pointer ">
               <h1 class="font-semi-bold text-xl font-mono text-white"><%=value%></h1>
            </a>
    <%})%>
    <%} else {%>
        <h1 class="text-center text-xl text-slate-200 m-10">No Tasks Created</h1>
        <%}%>
      </div>
    </div>
</body>
</html>